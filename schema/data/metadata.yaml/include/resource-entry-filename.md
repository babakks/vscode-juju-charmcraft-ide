<a href="#heading--resource.filename"><h2 id="heading--resource.filename">`[resource].filename`</h2></a>

**Status:** Required: when `type:file`.

**Purpose:** The filename of the resource as it should appear in the filesystem.

**Structure:** 

```yaml
filename: <filename>
```
